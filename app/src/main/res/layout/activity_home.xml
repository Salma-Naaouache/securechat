<?xml version="1.0" encoding="utf-8"?>
<androidx.coordinatorlayout.widget.CoordinatorLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#FFFFFF">

    <com.google.android.material.appbar.AppBarLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="#FFFFFF"
        app:elevation="0dp">

        <androidx.appcompat.widget.Toolbar
            android:id="@+id/toolbar"
            android:layout_width="match_parent"
            android:layout_height="97dp"
            app:title="Secure Chat"
            app:titleTextAppearance="@style/FunkyTitleStyle" />

        <FrameLayout
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            android:paddingHorizontal="20dp"
            android:paddingBottom="15dp">

            <EditText
                android:layout_width="match_parent"
                android:layout_height="48dp"
                android:background="@drawable/search_bg_minimal"
                android:backgroundTint="#B375E2"
                android:drawableStart="@android:drawable/ic_menu_search"
                android:drawablePadding="12dp"
                android:hint="Rechercher une discussion"
                android:paddingStart="16dp"
                android:textColorHint="#A69EB6"
                android:textSize="14sp"
                app:drawableTint="#EAE8ED" />
        </FrameLayout>
    </com.google.android.material.appbar.AppBarLayout>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/rvDiscussions"
        android:layout_width="match_parent"
        android:layout_height="605dp"
        android:clipToPadding="false"
        android:paddingBottom="80dp"
        app:layout_anchor="@+id/rvDiscussions"
        app:layout_anchorGravity="center"
        app:layout_behavior="@string/appbar_scrolling_view_behavior" />

    <com.google.android.material.bottomnavigation.BottomNavigationView
        android:id="@+id/bottomNavigation"
        android:layout_width="match_parent"
        android:layout_height="72dp"
        android:layout_gravity="bottom"
        android:background="#FFFFFF"
        app:elevation="20dp"
        app:itemIconTint="@color/navigation_colors"
        app:itemTextColor="@color/navigation_colors"
        app:menu="@menu/bottom_menu" />

    <com.google.android.material.floatingactionbutton.FloatingActionButton
        android:id="@+id/fabNewChat"
        android:layout_width="116dp"
        android:layout_height="wrap_content"
        android:layout_gravity="bottom|end"
        android:layout_marginEnd="20dp"
        android:layout_marginBottom="90dp"
        android:src="@android:drawable/ic_input_add"
        app:backgroundTint="#874DC1"
        app:elevation="6dp"
        app:pressedTranslationZ="12dp"
        app:tint="#FFFFFF" />

</androidx.coordinatorlayout.widget.CoordinatorLayout>